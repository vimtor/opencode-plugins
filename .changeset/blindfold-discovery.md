---
"opencode-blindfold": patch
---

Agents now use Blindfold on their own when a task needs a secret, instead of asking you to paste it into the chat. `blindfold_request` is a regular tool, so it also works for agents without Code Mode, and its description explains how to use the secret in shell commands and Code Mode.
