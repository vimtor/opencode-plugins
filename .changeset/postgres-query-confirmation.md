---
"opencode-postgres": patch
---

Use OpenCode's native `postgres_query` permission behavior, support session-wide **always** approval, identify read-only versus read/write mode in the tool description, and require the plugin API version that supports Promise-based tool permission prompts.
